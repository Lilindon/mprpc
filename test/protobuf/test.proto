syntax = "proto3"; // 声明protobuf的版本

package fixbug; // 声明代码所在的包 == namespace(c++)

// 定义登陆消息类型 name pwd
message LoginRequest
{
    bytes name = 1;   // 表示LoginRequest第一个字段
    bytes pwd = 2;    // 直接存成二进制文件 bytes字节
}

// 定义登陆响应消息类型
message LoginResponce
{
    int32 errcode = 1;
    bytes errmsg = 2;
    bool success = 3; 
}
